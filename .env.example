# Canonical production URL — powers og:image URLs, canonical links, sitemap.xml, robots.txt.
# On Vercel you can set this in the dashboard (Settings → Environment Variables); it's also
# auto-injected from the deployment domain. Override here only for local "production" smoke tests.
NEXT_PUBLIC_SITE_URL=https://git-in-depth.vercel.app